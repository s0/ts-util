console.log('hello world');

/**
 * Similar to Node.js's path.relative():
 * https://nodejs.org/api/path.html#path_path_relative_from_to
 */
function relative(_fromFolder: string, _toPath: string) {
  // const fromSplit = fromFolder.split('/');
  // const toSplit = toPath.split('/');
  // const root = { children: [] };
  // let current = root;
  // for (const part of fromFolder) {
  //   current.children =
  // }
  return '';
}

export = relative;
